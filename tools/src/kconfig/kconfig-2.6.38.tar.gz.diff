diff -ruN original/kconfig-2.6.38/scripts/kconfig/confdata.c kconfig-2.6.38/scripts/kconfig/confdata.c
--- original/kconfig-2.6.38/scripts/kconfig/confdata.c	2017-09-04 14:12:31.000000000 +0200
+++ kconfig-2.6.38/scripts/kconfig/confdata.c	2017-09-04 14:52:17.930577967 +0200
@@ -65,7 +65,7 @@
 {
 	char *name = getenv("KCONFIG_CONFIG");
 
-	return name ? name : ".config";
+	return name ? name : "config/system.conf";
 }
 
 const char *conf_get_autoconfig_name(void)
@@ -587,14 +587,14 @@
 		basename = conf_get_configname();
 
 	sprintf(newname, "%s%s", dirname, basename);
-	env = getenv("KCONFIG_OVERWRITECONFIG");
+	/*env = getenv("KCONFIG_OVERWRITECONFIG");
 	if (!env || !*env) {
 		sprintf(tmpname, "%s.tmpconfig.%d", dirname, (int)getpid());
 		out = fopen(tmpname, "w");
 	} else {
-		*tmpname = 0;
-		out = fopen(newname, "w");
-	}
+		*tmpname = 0;*/
+	out = fopen(newname, "w");
+	/*}*/
 	if (!out)
 		return 1;
 
@@ -651,7 +651,7 @@
 	}
 	fclose(out);
 
-	if (*tmpname) {
+	/*if (*tmpname) {
 		strcat(dirname, basename);
 		strcat(dirname, ".old");
 		rename(newname, dirname);
@@ -659,7 +659,7 @@
 			return 1;
 	}
 
-	conf_message(_("configuration written to %s"), newname);
+	conf_message(_("configuration written to %s"), newname);*/
 
 	sym_set_change_count(0);
 
diff -ruN original/kconfig-2.6.38/scripts/kconfig/mconf.c kconfig-2.6.38/scripts/kconfig/mconf.c
--- original/kconfig-2.6.38/scripts/kconfig/mconf.c	2017-01-02 05:17:56.000000000 +0100
+++ kconfig-2.6.38/scripts/kconfig/mconf.c	2017-09-04 14:52:38.812105730 +0200
@@ -846,15 +846,15 @@
 			return 1;
 		}
 	case -1:
-		printf(_("\n\n"
+		/*printf(_("\n\n"
 			"*** End of the configuration.\n"
 			"*** Execute 'make' to start the build or try 'make help'."
-			"\n\n"));
+			"\n\n"));*/
 		break;
-	default:
+	/*default:
 		fprintf(stderr, _("\n\n"
 			"Your configuration changes were NOT saved."
-			"\n\n"));
+			"\n\n"));*/
 	}
 
 	return 0;
